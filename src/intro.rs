//Data types
//Every value in rust is of a certain data type,which tells Rust what kind of data is being specified 
fn main() {
    //addition
    let sum = 5 + 10;

    //substraction 
    let difference = 95.5 - 4.3;

    // multiplication
    let product = 4 * 30;

    // division
    let quotient = 56.7 / 32.2;
    let truncated = -5 / 3; //Result in -1

    // remainder 
    let remainder = 43 % 5;

    //The Boolean Type
    let t = true;
    let f: bool = false; // with explicit type anotation

    //The Character Type
    let c = 'z';
    let z: char = 'Z';  //with explicit type anotation
    let 
}

